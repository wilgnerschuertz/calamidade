import 'package:flutter/material.dart';

class WantToHelpPage extends StatelessWidget {
  const WantToHelpPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('WantToHelpPage'),
      ),
      body: const Placeholder(),
    );
  }
}
